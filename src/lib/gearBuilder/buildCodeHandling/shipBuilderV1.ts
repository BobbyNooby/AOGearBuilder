import type { CurrentShipBuild } from "$lib/shipBuilder/ShipClass";
import type { anyItem } from "$lib/utils/itemTypes";


// Load Code Logic.

export function loadShipBuilderV1(ship :  CurrentShipBuild, database : anyItem[], buildCode : string) {
    try{
        
    }

}