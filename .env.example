# MongoDB (used by API)
MONGO_CONNECT_URL="mongodb+srv://.../aotools?retryWrites=true&w=majority"

# BetterAuth (used by API)
BETTER_AUTH_SECRET="change-me-in-production-32-characters-long-key"
BETTER_AUTH_URL="http://localhost:4000"

# Discord OAuth (used by API)
DISCORD_CLIENT_ID=""
DISCORD_CLIENT_SECRET=""

# Admin Discord IDs (comma-separated, used by API)
ADMIN_DISCORD_IDS=""

# Internal API key (used by web server → API)
INTERNAL_API_KEY="internal_super_secret_change_this_dev_only"

# CORS origins (used by API)
ALLOWED_ORIGINS="http://localhost:5173"

# Redis URL (optional — memory fallback if empty)
REDIS_URL=""

# Public API URL (used by web browser)
PUBLIC_API_URL="http://localhost:4000"

# Public web origin (Vite dev server default is 5173)
PUBLIC_ORIGIN="http://localhost:5173"
